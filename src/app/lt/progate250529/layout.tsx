import { Metadata } from "next";

export const metadata: Metadata = {
  description: "Progate LT会 Sansan出張版スライド",
}

export default function ProgateLT250529Layout({
  children
} : {
  children: React.ReactNode
}) {
  return children
}
